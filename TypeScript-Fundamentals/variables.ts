let a : number;
let b : string;
let c : boolean;
let d : any;
let e : number[] = [1,2,3];
console.log(e[0]);
console.log("num "+a+" str "+b+" bool "+c+" any "+d);
enum Animal{
    Dog,
    Cat,
    Cheetah,
    Butterfy
}
console.log(Animal.Butterfy,Animal.Cheetah,Animal.Butterfy);